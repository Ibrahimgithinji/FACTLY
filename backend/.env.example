# Example environment variables for FACTLY backend
# Copy this file to `.env` and fill in the correct values before running.

# Django Secret Key - MUST be unique and random in production
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=your-random-secret-key-here

# Debug mode (set to False in production)
DEBUG=True

# Allowed hosts (comma-separated for production)
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS origins (comma-separated) - default allows localhost:3000 for development
ALLOWED_CORS=http://localhost:3000

# Email Configuration (for password reset functionality)
# ----------------------------- IMPORTANT -----------------------------
# This file contains EXAMPLE values. Replace the placeholders below with
# real credentials before running in production. Do NOT commit real
# credentials to source control; use a secure secrets manager or CI/CD
# environment variables in production.
# --------------------------------------------------------------------

# Choose the email backend. For production use an SMTP backend (SendGrid,
# Mailgun, Gmail SMTP, SES, etc.). For development you can keep the
# console backend (prints emails to backend logs).
# Examples:
#  - Django SMTP backend: django.core.mail.backends.smtp.EmailBackend
#  - Console backend (dev): django.core.mail.backends.console.EmailBackend
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# SMTP server for sending emails
# Gmail (recommended: use an App password if using Gmail):
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# SMTP credentials (replace with real values)
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-smtp-password-or-app-password

# The "From" address shown in outgoing emails
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# Password Reset Configuration
# Token expiry (hours)
PASSWORD_RESET_TIMEOUT_HOURS=24

# Frontend URL used to construct reset links sent in emails
# In development: http://localhost:3000
# In production: https://your-frontend-domain.com
FRONTEND_URL=http://localhost:3000

# Alternative provider examples (uncomment and fill if using):
# SendGrid (API key as SMTP password)
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=YOUR_SENDGRID_API_KEY

# Mailgun (SMTP credentials)
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=postmaster@sandboxXXXX.mailgun.org
# EMAIL_HOST_PASSWORD=your-mailgun-password

# AWS SES (SMTP)
# EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=YOUR_SES_SMTP_USER
# EMAIL_HOST_PASSWORD=YOUR_SES_SMTP_PASSWORD


# Google Fact Check API
# Note: Google Fact Check API requires the API key as a query parameter (API design constraint).
# The key is NOT exposed in logs/history due to rate limiting wrapper.
GOOGLE_FACT_CHECK_API_KEY=your_google_api_key_here

# NewsLdr API (optional)
# Note: The API key is passed via Authorization header (Bearer token) to avoid logging exposure.
NEWSLDR_API_KEY=your_newsldr_api_key_here

# Application settings
CACHE_TTL=3600
MAX_API_REQUESTS_PER_MINUTE=60

# Security settings (for production)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
X_FRAME_OPTIONS=DENY

# Upload limits (bytes)
DATA_UPLOAD_MAX_MEMORY_SIZE=5242880
FILE_UPLOAD_MAX_MEMORY_SIZE=5242880
